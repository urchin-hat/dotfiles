# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
    . ~/.bashrc
fi

# --- User specific environment and startup programs ---
# 1. completion (補完機能の読み込み)
# if [ -f "$HOME/.asdf-completion.bash" ]; then
#    . "$HOME/.asdf-completion.bash"
# fi
